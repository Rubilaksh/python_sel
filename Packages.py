'''

Packages is a directory containing multiple modules and other sub packages.

Suppose, we are writing a large program--> we will split into different modules--> then we will group modules of similar type called package

game(packages)
player.py --> characters
enemy.py --> characters
boss.py --> characters
gun.py --> weapons
knife.py--> weapons

'''